{% extends "base.html.twig" %}

{% block title %}Contact
{% endblock %}

{% block body %}
	<div class=" form-control container mt-4">
			<div class="card-body p-5">
				<h1 class="fw-light">Nous contacter</h1>
				<p class="lead">Nous prendrons soin de vous répondre dans les meilleurs délais</p>

				{{ form_start(formContact, {
					attr: {novalidate: "novalidate"}
				}) }}

				{{form_widget(formContact)}}
				
				{{ form_end(formContact) }}

			</div>
	</div>


{% endblock %}
